import { CSSProperties, ReactNode } from 'react';
import { DevtoolsOptions } from 'zustand/middleware';
import { BackBottomProps } from '../../BackBottom';
import { ChatProps } from '../store';
import { ProChatChatReference } from './StoreUpdater';
export interface ProChatProps<T extends Record<string, any>> extends ChatProps<T> {
    renderInputArea?: (defaultDom: ReactNode, onMessageSend: (message: string) => void | Promise<any>, onClearAllHistory: () => void) => ReactNode;
    __PRO_CHAT_STORE_DEVTOOLS__?: boolean | DevtoolsOptions;
    showTitle?: boolean;
    style?: CSSProperties;
    className?: string;
    chatRef?: ProChatChatReference;
    appStyle?: CSSProperties;
    backToBottomConfig?: Omit<BackBottomProps, 'target'>;
}
export declare function ProChat<T extends Record<string, any> = Record<string, any>>({ renderInputArea, __PRO_CHAT_STORE_DEVTOOLS__, showTitle, style, className, chatItemRenderConfig, backToBottomConfig, appStyle, ...props }: ProChatProps<T>): import("react/jsx-runtime").JSX.Element;
