export declare class MockResponse {
    private data;
    private delay;
    private controller;
    private encoder;
    private stream;
    constructor(data: string, delay?: number);
    private pushData;
    getResponse(): Response;
}
